/**
 * Test-runner stub for Next.js's `server-only` package.
 *
 * `server-only` exists purely to make a build fail when a server module is
 * pulled into a client bundle; it has no runtime behaviour and no export map
 * Vite can resolve. Aliased here from vitest.config.ts so server modules that
 * guard themselves with it stay unit-testable.
 */
export {};

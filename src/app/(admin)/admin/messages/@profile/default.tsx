/**
 * Default content for the `@profile` parallel slot. Renders nothing when the
 * URL doesn't match the intercepting profile route — keeps the Sheet closed.
 */
export default function ProfileSlotDefault() {
  return null;
}
